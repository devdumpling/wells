{
  "name": "@pal/tokens",
  "version": "0.0.1",
  "description": "Tokens for the Pal design system.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist && rimraf node_modules",
    "build": "run-p build:dist build:types",
    "build:dist": "swc lib -d dist --source-maps",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist"
  },
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "keywords": [
    "design system",
    "tokens",
    "react",
    "typescript"
  ],
  "author": "Devon Wells",
  "license": "MIT",
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.24",
    "eslint-config-custom": "workspace:*",
    "npm-run-all": "^4.1.5",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.2"
  }
}
