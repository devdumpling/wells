{
  "name": "tsconfig",
  "version": "0.0.2",
  "private": true,
  "files": [
    "base.json",
    "nextjs.json",
    "react-library.json"
  ],
  "scripts": {
    "clean": "rimraf node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
