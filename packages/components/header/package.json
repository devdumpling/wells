{
  "name": "wells.components.header",
  "version": "0.0.1",
  "scripts": {
    "build": "turbo build",
    "clean": "rimraf node_modules",
    "lint": "eslint --ext .ts src",
    "lint:fix": "eslint --ext .ts src --fix",
    "prettier": "prettier --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
